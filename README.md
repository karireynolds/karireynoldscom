# karireynolds.com

Personal site — About + Resume, single-page scroll. Static HTML/CSS/JS, deployed on Vercel.

## Local preview

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000
